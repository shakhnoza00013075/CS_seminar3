def any_base_to_decimal( number, base):
    temp=int(number, base)
    print(temp)

u_number=input("Enter a digit to convert")
u_base = int(input("Enter a base"))
any_base_to_decimal(u_number, u_base)
